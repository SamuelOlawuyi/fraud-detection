"""Additional API routes (for future extensions)."""

from fastapi import APIRouter

router = APIRouter()

# Add additional routes here as needed
# Example: statistics, model retraining endpoints, etc.